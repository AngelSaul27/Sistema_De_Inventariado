export const FieldsContainer = ({children}) => (
    <div className={"flex gap-4"}>
        {children}
    </div>
)
export const FieldSpace = ({children}) => (
    <div className={"space-y-4 flex-1"}>
        {children}
    </div>
)
